# 12. 앞으로 할 것

**2026-08-25 기준.** 지금 상태를 훑고, 실제로 값을 하는 것만 골라 적었습니다.

관련 문서 — 결정 기록 `00` §1 · 기능 목록 `01` · 화면 규격 `11`

---

## 지금까지 된 것

| | 상태 |
|---|---|
| 회원가입·로그인 (사원 번호 / 관리자 아이디) | ✅ |
| 120문항 설문 · 이어하기 | ✅ |
| 개인 결과 (7축 · 하위척도 · 짝 해석) | ✅ |
| 관리자 첫 화면 | ✅ |
| 구성원 목록·상세 (정렬 11가지, 펼침 그래프) | ✅ |
| 분석 — 관련도 표 · 순위 · 예측 대 실제 · 검사 신뢰도 | ✅ |
| 관리자 평가 입력 (3축 × 1~10) | ✅ Task 30 |
| 직무능력 값 출처 전환 (직원 / 관리자 / 평균) | ✅ D-39 |
| 응답 신뢰도 판정 · 사람별 순위 | ✅ |
| 다크 모드 | ✅ |

**아직 제 노트북에서만 돕니다.** 사내 서버에 안 올렸습니다.

---

## 1군 — 없으면 운영을 못 합니다

### ✅ 1-1. 랜딩·고지 화면 `Task 27`

사원이 링크를 받고 들어오면 **로그인 상자만** 보입니다. 이게 뭔지, 왜 하는지, 내 답을 누가 보는지 아무 설명이 없습니다.

들어가야 할 것 — 무엇을 재는 검사인지 · 몇 분 걸리는지 · **누가 무엇을 보는지**(관리자는 보고 동료는 못 본다) · 개인정보 고지 · 문항이 검증 중이라는 안내(`01` §3.4).

**이거 없이는 사내 공지를 못 냅니다.**

### ✅ 1-2. 문항 목록 화면 `Task 28`

관리자가 무슨 문항이 나가는지 볼 방법이 없습니다. 지금은 `data/items/v1.yaml`을 열어야 합니다.

관리자가 "무슨 질문 하는데?"라고 물으시면 **보여드릴 화면이 없습니다.**

### ⬜ 1-3. 사내 서버 배포 ⭐ `Task 27`

서버 정보(주소·OS·Docker 여부)를 주시면 준비하겠습니다. DB 백업 절차도 이때 같이 잡아야 합니다.

---

## 2군 — 데이터를 믿게 만듭니다

### 🟡 2-1. 재검사 — 되지만 비교가 없음 (C-5)

같은 사람이 **몇 주 뒤 다시 응시**하는 기능입니다. DB는 이미 여러 세션을 받게 돼 있는데(`TestSession`) 화면이 없습니다.

**왜 중요한가** — 검사가 믿을 만한지 보여주는 가장 강한 증거가 **재검사 일치도**입니다. 지금 우리가 가진 α는 「문항끼리 맞물리나」만 말하고, 「같은 사람이 다시 하면 같게 나오나」는 답을 못 합니다.

관리자가 의심하실 때 내밀 수 있는 유일한 숫자입니다.

### ⬜ 2-2. 문항별 통계

α는 「이 척도가 안 좋다」까지만 말하고 **어느 문항이 문제인지**는 안 알려줍니다.

문항별로 두 가지를 보면 고칠 문항이 특정됩니다.

- **다들 같게 답하는가** — 변별이 안 되는 문항
- **총점과 따로 노는가** — 다른 것을 재고 있는 문항 (item-total 상관)

`v2` 문항 개정의 근거가 됩니다.

---

## 3군 — 관리자가 실제로 씁니다

### ✅ 3-1. 평가 대조 화면 `Task 31`

지금은 출처 전환기로 **간접적으로만** 볼 수 있습니다. 전용 화면이 필요합니다.

- 축별 일치도 (본인 답 ↔ 관리자 평가)
- **가장 크게 갈리는 사람 목록**
- 어느 축에서 갈렸는지

관리자 평가를 넣은 **원래 목적**입니다(`D-34`). 첫 화면 구석에 숫자 세 개로 줄이면 오히려 오해를 부릅니다 — 자기 화면을 가져야 합니다.

### ⬜ 3-2. 두 사람 나란히 보기

"이 둘을 같은 팀에 두면 어떨까"를 볼 화면입니다. 지금은 창을 두 개 띄워야 합니다. 배치·짝 만들 때 실제로 쓰실 것 같습니다.

### 🟡 3-3. 결과 내보내기 — CSV·인쇄는 됨, 묶음 PDF 는 없음

회의 자료로 뽑거나 나중에 다른 도구로 볼 때. 만들기 쉽고 오래 갑니다.

---

## 4군 — 사람이 쌓이면

### ⬜ 4-1. 부서 정보 채우기

`Employee.department` 칸이 있는데 안 채우고 있습니다. 채우면 부서별 비교가 열립니다.

⚠️ **50명이면 부서당 5~10명**이라 통계로는 약합니다. 「참고」 수준으로만 쓸 수 있다고 화면에 못 박아야 합니다. `00`에서 부서별 비교를 한 번 제외했던 이유가 이것입니다.

### ⬜ 4-2. 사내 규준 쌓기

지금 눈금은 **문항 중간값**이 50입니다(`D-26`). 바깥 기준이 없어서 「평균이 무의미하다」는 문제가 여기서 나왔습니다.

사람이 30~50명 쌓이면 **우리 회사 기준**으로 바꿀 수 있습니다. 그때 「사내 상위 20%」가 말이 됩니다.

---

## 5군 — 안전·방침

### ✅ 5-1. 퇴사자 처리

**완전 삭제** (2026-08-25, `D-48`). 구성원 상세 맨 아래에 있습니다.

「잠그기」와 「비식별 보존」은 만들지 않습니다 — 앞엣것은 목적이 끝났는데 계속 보관하는 것이고, 뒤엣것은 50명 규모에서 점수만으로 누구인지 짐작되어 익명이 아닙니다.

### ✅ 5-2. 미완료 세션 정리 기간 `Task 29`

**14일로 확정** (2026-08-25, `D-47`). 세션 상태만 「중단」으로 바뀌고 계정·비밀번호·기존 결과는 그대로입니다.

### 5-3. DB 백업 — ✅ 완료 (2026-08-25, D-114)

자동 덤프(하루 한 벌·14벌 보관)와 관리자 내려받기 버튼을 붙였습니다. 복구 절차와 실제로 복구해 본 결과는 `13-backup-restore.md`에 있습니다.

---

## 안 만드는 게 낫다고 보는 것

기록으로 남깁니다. 나중에 누가 "이건 왜 없지?" 할 때 답이 되게.

| | 왜 |
|---|---|
| **감사 기록** (누가 누구 결과를 봤나) | 관리자가 관리자 한 분입니다. 자기가 자기를 감시하는 기록이 됩니다 (`D-29`에서 이미 제외) |
| **알림·메일** | 50명이면 카톡 공지가 빠릅니다 |
| **권한 등급 세분화** | 사원 / 관리자 둘로 충분합니다 |
| **모바일 전용 앱** | 브라우저로 됩니다. 리커트는 좁은 화면에서 세로 목록으로 바뀝니다 |
| **AI 자동 해석** | **근거 없는 문장이 나옵니다.** 이 프로젝트가 계속 피해온 것입니다 — 큐브 해석을 뺀 이유(`00` §2.2)와 같습니다 |

---

## 순서 추천

```
관리자에게 보여드리기 전   1-1  랜딩·고지
보여드린 직후           3-1  평가 대조     ← 평가를 다 매기시면 바로 볼 게 있어야 합니다
사내 공개 전            1-2  문항 목록
                       1-3  배포
                       5-1  퇴사자 방침
한 달쯤 뒤              2-1  재검사
                       2-2  문항별 통계
```

**3-1을 두 번째에 둔 이유** — 관리자가 132칸을 다 채우신 다음에 볼 화면이 없으면, 그 수고가 어디에 쓰였는지 안 보입니다.

---

# 코드를 훑고 나온 것 (2026-08-25)

위 목록은 **대화하면서 겪은 것**을 적은 것이었습니다. 그다음에 저장소를 직접 뒤졌습니다 — 안 불리는 함수, 안 쓰는 스키마 칸, 오류 처리가 빠진 길, 테스트 없는 모듈, 문서와 코드가 어긋난 곳.

**나온 것 중 값이 있는 것만** 적습니다. "이론적으로는 있으면 좋은 것"은 뺐습니다.

## 🔴 고쳐야 합니다

> **2026-08-25 — C-1 · C-3(일부) · C-6 · C-7 처리 완료.** 아래는 무엇이 문제였는지 남겨 둔 기록입니다.

### ✅ C-1. 설문 저장·제출에 오류 처리가 없습니다

`src/lib/survey/actions.ts` — `try` 블록이 **0개**입니다.

로그인 쪽은 `guarded()`로 감쌌는데(2026-08-24) **설문 쪽은 안 했습니다.** DB가 끊기거나 저장이 실패하면 `saveSectionAction`·`submitAction`이 예외를 던지고, 화면 쪽 `useTransition`은 그걸 잡지 않습니다.

**120문항을 다 푼 사람이 제출 버튼을 눌렀는데 아무 일도 안 일어납니다.** 오류 문구도, 다시 시도하라는 안내도 없습니다.

세 화면 중 **실패가 가장 아픈 곳**인데 방어가 가장 얇습니다. 로그인은 다시 누르면 되지만 이건 20분을 날립니다.

### ✅ C-2. `sweepStaleSessions`가 사실상 꺼져 있습니다

함수는 있고 `getOrCreateSession`에서 부르기도 합니다. 그런데 `STALE_SESSION_DAYS = null`이라 **항상 즉시 반환**합니다.

기간을 안 정해서 그랬습니다. **2026-08-25에 14일로 정해 켰습니다** (`D-47`).

같이 나온 것 — 화면의 상태 표에 `ABANDONED`가 빠져 있어 **빈 칸으로 나오고 있었습니다.** 「중단」으로 채웠고, 거르개의 「미응시만」도 「미응시·중단만」으로 묶었습니다.

### 🟡 C-3. 핵심 모듈에 테스트가 없습니다 — 하나 남음

| 모듈 | 무엇을 하나 | 테스트 |
|---|---|---|
| `auth/password.ts` | 비밀번호 해시·검증 | ✅ 2026-08-25 (5개) |
| `auth/rateLimit.ts` | 로그인 실패 잠금 | ✅ 2026-08-25 (6개) |
| `survey/session.ts` | 세션 생성·이어하기·구간 이동 | ✅ 2026-08-26 (17개, `*.db.test.ts`) |
| `survey/result.ts` | 채점 결과 저장 | **✗ 남음** |

점수 계산(`scoring/`)과 통계(`admin/stats`)는 잘 덮여 있고, 세션은 시험용 DB 를 따로 세워 덮었습니다(`15-테스트.md`).

**남은 것은 `result.ts` 하나입니다.** 여기가 잘못되면 다 푼 응시의 채점 결과가 안 남거나 잘못 남습니다. 세션 테스트와 같은 방식(`*.db.test.ts`)으로 붙이면 됩니다.

## 🟡 정리하면 좋습니다

### C-4. 아무도 안 쓰는 스키마

| 칸 | 상태 |
|---|---|
| `Roster` 표 전체 | **0곳에서 사용.** D-30에서 「참고용 명단, P1」로 남긴 것 |
| `Employee.employeeNo` | 0곳 |
| `Employee.department` | 0곳 — `4-1`에서 쓰자고 한 그 칸 |
| `Employee.rosterId` | 0곳 |
| `TestSession.departmentAtTime` | 0곳 |
| `Result.normBasisN` | **0곳.** 「몇 명 기준으로 상대위치를 냈는가」를 담으려던 칸인데 아무도 안 씀 |

지우자는 게 아닙니다. **쓸 계획이 있는 것과 없는 것을 갈라 적어야** 나중에 헷갈리지 않습니다. `department`는 `4-1`에서 쓸 것이고, `normBasisN`은 `4-2`(사내 규준)와 묶입니다. `Roster`·`employeeNo`·`rosterId`·`departmentAtTime`은 **쓸 자리가 안 보입니다.**

### C-5. 재검사는 되는데 비교가 없습니다

「다시 응시하기」 링크가 있고 새 세션도 제대로 만들어집니다. 그런데—

- 관리자 목록은 `take: 1`로 **가장 최근 것만** 봅니다
- 이전 결과와 나란히 놓는 화면이 **없습니다**
- 재검사 일치도를 계산하는 코드가 **없습니다**

`2-1`이 "만들어야 한다"가 아니라 **"절반은 이미 있고 비교만 없다"**입니다. 생각보다 작은 일입니다.

### ✅ C-6. 결과를 인쇄할 수 없습니다

`@media print` 규칙이 **0개**입니다. 개인 결과 화면을 인쇄하면 상단 메뉴와 다크 모드 배경이 그대로 나옵니다.

사원이 자기 결과를 종이로 갖고 싶어 할 수 있고, 관리자가 회의에 뽑아 가실 수 있습니다. `3-3`(내보내기)의 **훨씬 싼 판**입니다 — CSS 한 뭉치면 됩니다.

### ✅ C-7. 설문 화면에 보조 표시가 없습니다

`LikertScale`에는 `aria-`/`sr-only`가 8곳 있는데 `SectionForm`에는 **0곳**입니다. 진행 상황("몇 번째 묶음, 몇 문항 남음")이 화면에는 보이지만 **읽어주는 표시가 없습니다.**

50명 중 한 명이라도 화면 낭독기를 쓴다면 걸립니다.

## ⚪ 알아두기만 하면 됩니다

- **비밀번호 찾기** — 사원이 스스로 못 합니다. 로그인 화면에 "관리자에게 요청" 안내가 있고 관리자 초기화 기능도 있습니다. **50명 규모에서는 이게 맞습니다.** 문자 인증을 붙일 값이 없습니다.
- **`snapshotJson`** — 1곳에서 쓰입니다(저장). 읽는 곳이 없는데, D-09에서 「결과는 채점 시점 값으로 고정」을 위해 남긴 것이라 **보험으로 두는 게 맞습니다.**

## 이번 감사에서 나오지 않은 것

**찾아봤지만 문제가 없던 것들**입니다. 다음에 같은 것을 또 뒤지지 않도록 적어 둡니다.

- `TODO`·`FIXME` 주석 — 없음
- 관리자 화면 권한 — 모든 경로가 로그인 없이는 튕김 (직접 확인)
- 비밀번호 — 해시로만 저장, 어느 화면에도 안 나옴
- 이어하기 — 세션·구간 단위로 동작함
- 설문 문항 유출 — 화면에 넘기는 필드에서 `isReverse`를 빼 둠

---

## 순서에 넣으면

```
✅ 끝              C-1  설문 오류 처리
                  C-6  인쇄 스타일
                  C-7  설문 보조 표시
                  C-3  비밀번호·잠금 테스트 (11개)

관리자에게 보여드리기 전  1-1  랜딩·고지
보여드린 직후          3-1  평가 대조
사내 공개 전           1-2  문항 목록
                     1-3  배포
한 달쯤 뒤             C-5  재검사 비교
                     2-2  문항별 통계
                     C-3  세션·채점 테스트 (남은 절반)
```

**C-1이 맨 앞이었던 이유** — 사내에 공지를 내는 순간 50명이 동시에 설문을 답니다. 그때 저장이 한 번 실패하면 그 사람은 무슨 일이 일어났는지 모릅니다.

## 2026-08-25에 한 것

| | 무엇을 했나 |
|---|---|
| **C-1** | `survey/actions.ts`에 `guarded()` — 로그인 쪽과 같은 규칙. 화면 쪽도 저장 실패에서 **멈추고 알리게** 고침. 전에는 저장이 실패해도 그냥 제출로 넘어갔다 |
| **덤** | `admin/actions.ts`의 비밀번호 초기화도 같이 감쌌다. 예외를 던져 화면에 원문이 찍히던 것을 문장으로 바꿈 |
| **C-6** | `@media print` — 어두운 화면이어도 **종이는 흰 바탕에 검은 글씨**. 메뉴·버튼 숨김, 표·그래프가 쪽 사이에서 안 잘리게 |
| **C-7** | 설문에 `aria-live` 진행 안내와 문항별 「답함/아직」 표시 |
| **C-2** | 세션 정리를 **14일**로 켰다 (`D-47`). 화면에서 빈 칸으로 나오던 「중단」 상태도 채움 |
| **C-3** | `password` 5개 · `rateLimit` 6개. **「정말 5번에 잠기는가」를 이제 기계가 확인한다** |

테스트 139 → **150개**.

## 2026-08-25 · 나중에 — 직무능력 문항 다시 쓰기

α가 기준 아래인 두 축을 **문항부터 손봐야** 합니다. 사람이 늘어도
좋아지지 않는 종류의 문제입니다.

| 축 | α | 문제 |
|---|---|---|
| 협력 | **.08** | 세 문항이 서로 다른 셋을 잰다 (문항끼리 평균 상관 `.03`) |
| 조직생활 | **.54** | 둘은 대인관계, 하나는 잔류 의도 |
| 자율적 실행 | .66 | 셋이 대체로 같은 방향. 문항을 늘리면 오른다 |

**협력 세 문항이 실제로 재는 것**

| | 문항 | 재는 것 |
|---|---|---|
| AB-COOP-01 | 여러 사람과 하는 일에서 내 몫을 잘 해내는 편이다 | 성실성 |
| AB-COOP-02 | 일이 밀릴 것 같으면 미리 동료에게 알린다 | 보고 습관 |
| AB-COOP-03 | 내 담당이 아니어도 옆에서 막히면 한 번은 들여다본다 | 자발적 도움 |

문항 사이 상관 `+.13 / −.16 / +.11` — **1번에 높게 답한 사람이 3번에는
오히려 낮게 답한다.**

**⚠️ 문항 수를 늘리는 것이 먼저가 아니다.** 지금 문항 그대로 열 개를
만들어도 α는 `.24`다. 순서는 이렇다:

1. **「협력」을 셋 중 무엇으로 정할지** 사용자가 결정 (정의는 데이터가
   아니라 회사가 정하는 것이다)
2. 그 하나를 재는 문항 5~6개를 새로 쓴다
3. 「조직생활」에서 잔류 의도(AB-ORG-03)를 어떻게 할지 정한다 — 빼거나,
   따로 축을 세우거나

참고: 인내력은 15문항 α `.92`, 역산한 문항 간 평균 상관 `.43`. TCI는
수십 년 다듬은 문항이라 그렇다 — 처음 쓴 문항이 그럴 수는 없다.

**관련 화면**: 이 문제 때문에 상관표의 협력·조직생활 열은 빗금 + 흐리게
(D-53, D-62), 관련 항목 카드도 흐리게 (D-79), 논문 대조에서는 판정
자체를 하지 않는다 (D-66).
