# 다이어그램 샘플 — 렌더링 확인용

> 이 파일은 **Mermaid 다이어그램이 보이는지 확인**하기 위한 것입니다.
> 아래 블록이 그림으로 보이면 Mermaid를 쓸 수 있고, 코드 텍스트로 보이면 ASCII로 갑니다.
>
> **확인 방법**: VS Code에서 이 파일을 열고 `Ctrl+Shift+V` (미리보기). 또는 GitHub에 올려서 보기.
> VS Code 1.121부터는 확장 설치 없이 기본 지원됩니다. 그 이전 버전이면 `bierner.markdown-mermaid` 확장을 설치하세요.

---

## 0. 먼저 — 렌더러 버전 확인

아래 블록은 **렌더링하는 쪽의 Mermaid 버전을 그림으로 출력**합니다.
사내 GitHub에 이 파일을 올린 뒤 이 블록에 찍힌 번호를 확인하세요. 그 번호가 우리가 쓸 수 있는 문법의 상한선입니다.

```mermaid
info
```

| 확인된 버전 | 쓸 수 있는 것 |
|---|---|
| **11.16 이상** | `swimlane-beta` 포함 전부 |
| **11.0 ~ 11.15** | 아래 5종 + `block-beta`, `architecture-beta` |
| **10.x 이하** | 아래 5종만 (이 문서의 모든 다이어그램은 여기 해당) |

> 이 문서의 다이어그램은 전부 **mermaid 10.9.8과 11.17.0 양쪽 파서에서 검증**되었습니다.
> 즉 사내 GitHub 버전이 무엇이든 깨지지 않습니다.

---

## 1. 전체 사용자 흐름

```mermaid
flowchart TD
    A["랜딩 · 고지문"] --> B["로그인<br/>이름 + 전화번호"]
    B --> C{"진행 중 세션이<br/>있나?"}
    C -->|"없음"| D["새 세션 생성"]
    C -->|"있음"| E["이어하기"]
    C -->|"이미 완료"| M["내 결과"]
    D --> F["섹션 1 ~ 7"]
    E --> F
    F --> G{"모든 섹션<br/>완료?"}
    G -->|"아니오"| F
    G -->|"예"| H["제출"]
    H --> I["서버 채점"]
    I --> J["품질 지표 계산"]
    J --> K["스냅샷 저장"]
    K --> M
    M --> N["언제든 재조회<br/>숫자 불변"]

    style H fill:#171717,color:#fff
    style M fill:#171717,color:#fff
```

---

## 2. 채점 파이프라인

```mermaid
flowchart LR
    A["응답 105개"] --> B["완결성 검증"]
    B -->|"미응답 있음"| X["반려"]
    B -->|"전부 응답"| C["역채점 적용<br/>8 - value"]
    C --> D["하위척도 합산"]
    D --> E["7대척도 합산"]
    E --> F["구간 판정<br/>lower / middle / upper"]
    F --> G["스냅샷 생성"]
    C --> H["품질 지표<br/>응답시간 + 역채점 일치도"]
    H --> I["quality_flags"]
    G --> J[("results")]

    style X fill:#dc2626,color:#fff
    style J fill:#171717,color:#fff
    style I fill:#171717,color:#fff
```

---

## 3. 권한 분기

```mermaid
flowchart TD
    A["요청"] --> B{"세션 쿠키<br/>있나?"}
    B -->|"없음"| C["/login 리다이렉트"]
    B -->|"있음"| D{"만료됐나?"}
    D -->|"예"| C
    D -->|"아니오"| E{"role?"}
    E -->|"USER"| F{"본인 데이터?"}
    E -->|"ADMIN"| G["전체 접근 허용"]
    F -->|"예"| H["허용"]
    F -->|"아니오"| I["403"]
    G --> J["감사 로그 기록"]
    J --> H

    style I fill:#dc2626,color:#fff
    style J fill:#f59e0b
```

---

## 4. 응시 화면 상태 머신

```mermaid
stateDiagram-v2
    [*] --> 시작전
    시작전 --> 섹션진행중 : 고지문 확인
    섹션진행중 --> 섹션진행중 : 다음 섹션
    섹션진행중 --> 미응답확인 : 마지막 섹션 완료
    미응답확인 --> 섹션진행중 : 미응답 있음
    미응답확인 --> 채점중 : 전부 응답
    채점중 --> 완료
    완료 --> [*]

    섹션진행중 --> 중단 : 브라우저 종료
    중단 --> 섹션진행중 : 재로그인 후 이어하기
```

---

## 5. 데이터 모델 관계

```mermaid
erDiagram
    Employee ||--o{ TestSession : "응시"
    Employee ||--o{ AuthSession : "로그인"
    Assessment ||--o{ Item : "문항"
    Assessment ||--o{ TestSession : "버전 고정"
    TestSession ||--o{ Response : "응답"
    TestSession ||--|| Result : "결과"
    TestSession ||--|| QualityFlag : "품질"
    Item ||--o{ Response : ""

    Employee {
        string name
        string phone
        enum role
    }
    Item {
        string code
        int section
        string scale
        string subscale
        bool isReverse
        string sourceRef
    }
    Result {
        json scoresJson
        json snapshotJson
        int normBasisN
    }
```

---

## 6. 응시 · 제출 시퀀스

> 역할별로 "누가 무엇을 하는가"를 보여줍니다. 스윔레인 대신 쓰는 표준 방식입니다 (아래 부록 참고).

```mermaid
sequenceDiagram
    autonumber
    participant U as 응시자
    participant W as 웹앱
    participant API as 세션 API
    participant SC as 채점 엔진
    participant DB as DB

    U->>W: 섹션 응답 입력
    W->>API: PATCH /session/:id/responses
    API->>DB: upsert Response
    DB-->>API: ok
    API-->>W: 저장 완료
    Note over W,API: 섹션 단위 자동 저장

    U->>W: 제출
    W->>API: POST /session/:id/submit
    API->>API: 완결성 검증 (105문항)
    alt 미응답 있음
        API-->>W: 422 미응답 목록
        W-->>U: 미응답 화면으로 이동
    else 전부 응답
        API->>SC: 채점 요청
        SC->>SC: 역채점 · 하위척도 · 7대척도
        SC->>SC: 품질 지표 계산
        SC-->>API: 스냅샷
        API->>DB: Result + QualityFlag 저장
        API-->>W: 201 결과 ID
        W-->>U: 내 결과 화면
    end
```

---

## 7. 구현 순서 (계획 문서의 15개 태스크)

```mermaid
flowchart TD
    T1["1. 스캐폴드 + DB"] --> T2["2. 문항 로더"]
    T2 --> T3["3. 문항 105개 작성"]
    T3 --> T4["4. seed"]
    T3 --> T5["5. 채점 엔진"]
    T5 --> T6["6. 품질 지표"]
    T1 --> T7["7. 인증"]
    T4 --> T8["8. 세션 API"]
    T7 --> T8
    T8 --> T9["9. 리커트 컴포넌트"]
    T9 --> T10["10. 섹션 응시 화면"]
    T5 --> T11["11. 제출 파이프라인"]
    T6 --> T11
    T10 --> T11
    T11 --> T12["12. 결과 화면"]
    T7 --> T13["13. 관리자 목록"]
    T12 --> T14["14. 개인 상세 + 감사"]
    T13 --> T14
    T14 --> T15["15. 랜딩 + Docker"]

    style T3 fill:#f59e0b
    style T5 fill:#171717,color:#fff
    style T15 fill:#171717,color:#fff
```

> 주황색 = 콘텐츠 작업 (사람 검토 필요) · 검정 = 핵심 검증 지점

---

## ASCII 대안

Mermaid가 안 보이면 이런 식으로 대체합니다. 어디서든 보이지만 표현력은 낮습니다.

```
로그인
  │
  ├─ 진행 중 세션 없음 ──→ 새 세션 ──┐
  ├─ 진행 중 세션 있음 ──→ 이어하기 ─┼──→ 섹션 1~7 ──→ 제출
  └─ 이미 완료 ─────────────────────┼───────────────────┐
                                     │                    ▼
                                     └──────────────→ 내 결과
```

> 한글은 글자 폭 계산이 어긋나기 쉬우므로 ASCII 아트는 **아주 단순한 구조에만** 쓰세요.
