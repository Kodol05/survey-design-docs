node_modules/

# (2026-08-23) 구 계획서의 개발용 DB 비밀번호를 자리표시자로 바꾸고, 저장소를 비공개로 전환하여
# 계획 문서를 버전 관리에 포함시킴. 비밀값은 .env 로만 두고 절대 커밋하지 않는다.

# DB 덤프 — 전화번호와 비밀번호 해시가 다 들어 있다. 절대 커밋하지 않는다
backups/
web/backups/

# 문서용 화면 사진 — HTML 안에 통째로 담기므로 따로 커밋하지 않는다
docs/shots/*.jpg

# 워드 문서 — .md 에서 다시 만들 수 있다 (docs/shots/build-docx.py)
# 만들어지는 문서 — 원본 .md 에서 docs/shots 의 스크립트로 다시 만든다.
# 접속 주소가 박혀 나오므로 커밋하지 않는다.
docs/*.docx
docs/화면안내.html
docs/shots/site-url.txt
.vercel
