/*
  `server-only` 자리에 끼우는 빈 파일 (테스트 전용).

  진짜 `server-only`는 불러오기만 하면 던진다 — 서버 전용 코드가 브라우저
  번들에 섞이는 것을 막는 장치다. 테스트는 서버에서만 도니 그 장치가
  필요 없고, 그대로 두면 파일을 열지도 못한다.

  ⚠️ 이건 `vitest.db.config.mts`에서만 끼운다. 앱 빌드는 진짜를 쓴다.
*/
export {};
