-- 평가·결과 국면 제거 (D-41).
--
-- 평가가 끝나기 전에는 관리자가 결과 화면에 못 들어가게 막던 장치다.
-- 걱정 자체는 맞지만 방식이 값을 못 치렀다 — 화면 절반이 잠긴 채로
-- 운영해야 했고, 한 번 열면 되돌릴 수 없었다.
-- 이제는 첫 화면에서 남은 인원만 알리고 판단은 사람이 한다.
--
-- AppState는 이 깃발 하나만 들고 있던 표라 통째로 지운다.
DROP TABLE "AppState";
DROP TYPE "RatingPhase";
